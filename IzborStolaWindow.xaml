<Window x:Class="Projekat_A_KafeBar.IzborStolaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Projekat_A_KafeBar"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        Title="{DynamicResource IzborStola_WindowTitle}" Height="350" Width="300"
        Background="{DynamicResource BackgroundBrush}">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Naslov -->
        <TextBlock Text="{DynamicResource IzborStola_Label}" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"
                   Foreground="{DynamicResource ForegroundBrush}"/>

        <!-- Lista stolova -->
        <ListBox x:Name="StoListBox" Grid.Row="1" DisplayMemberPath="Opis"
                 Background="{DynamicResource BackgroundBrush}"
                 Foreground="{DynamicResource ForegroundBrush}"
                 BorderBrush="{DynamicResource BorderBrush}"/>

        <!-- Dugmad -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="SelectButton" Content="{DynamicResource IzborStolaButton_Izaberi}" Width="100" Height="30" Margin="0,0,10,0"
                    Background="{DynamicResource PrimaryBrush}" Foreground="{DynamicResource ForegroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}" Click="SelectButton_Click"/>
            <Button x:Name="CancelButton" Content="{DynamicResource IzborStolaButton_Otkazi}" Width="100" Height="30"
                    Background="{DynamicResource SecondaryBrush}" Foreground="{DynamicResource ForegroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
