<Window x:Class="Projekat_A_KafeBar.DodajIzmijeniProizvodWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Projekat_A_KafeBar"
        mc:Ignorable="d"
        Title="{DynamicResource Proizvod_WindowTitle}"
        Height="250" Width="400"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Naziv -->
        <TextBlock Text="{DynamicResource Proizvod_Naziv}"
                   Grid.Row="0"
                   Grid.Column="0"
                   VerticalAlignment="Center"
                   Foreground="{DynamicResource TextBrush}"/>

        <TextBox x:Name="NazivBox"
                 Grid.Row="0"
                 Grid.Column="1"
                 Margin="0,5"
                 Background="{DynamicResource SecondaryBrush}"
                 Foreground="{DynamicResource TextBrush}"
                 BorderBrush="{DynamicResource PrimaryBrush}"/>

        <!-- Cijena -->
        <TextBlock Text="{DynamicResource Proizvod_Cijena}"
                   Grid.Row="1"
                   Grid.Column="0"
                   VerticalAlignment="Center"
                   Foreground="{DynamicResource TextBrush}"/>

        <TextBox x:Name="CijenaBox"
                 Grid.Row="1"
                 Grid.Column="1"
                 Margin="0,5"
                 Background="{DynamicResource SecondaryBrush}"
                 Foreground="{DynamicResource TextBrush}"
                 BorderBrush="{DynamicResource PrimaryBrush}"/>

        <!-- Količina -->
        <TextBlock Text="{DynamicResource Proizvod_Kolicina}"
                   Grid.Row="2"
                   Grid.Column="0"
                   VerticalAlignment="Center"
                   Foreground="{DynamicResource TextBrush}"/>

        <TextBox x:Name="KolicinaBox"
                 Grid.Row="2"
                 Grid.Column="1"
                 Margin="0,5"
                 Background="{DynamicResource SecondaryBrush}"
                 Foreground="{DynamicResource TextBrush}"
                 BorderBrush="{DynamicResource PrimaryBrush}"/>

        <!-- Kategorija -->
        <TextBlock Text="{DynamicResource Proizvod_Kategorija}"
                   Grid.Row="3"
                   Grid.Column="0"
                   VerticalAlignment="Center"
                   Foreground="{DynamicResource TextBrush}"/>

        <ComboBox x:Name="KategorijaComboBox"
                  Grid.Row="3"
                  Grid.Column="1"
                  Margin="0,5"
                  Height="25"
                  Background="{DynamicResource SecondaryBrush}"
                  Foreground="{DynamicResource TextBrush}"
                  BorderBrush="{DynamicResource PrimaryBrush}"/>

        <!-- Dugmad -->
        <StackPanel Orientation="Horizontal"
                    Grid.Row="4"
                    Grid.ColumnSpan="2"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">

            <Button Content="{DynamicResource Proizvod_Btn_Sacuvaj}"
                    Width="80"
                    Height="28"
                    Margin="0,0,10,0"
                    Background="{DynamicResource PrimaryBrush}"
                    Foreground="{DynamicResource TextOnPrimaryBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    Click="Sacuvaj_Click"/>

            <Button Content="{DynamicResource Proizvod_Btn_Otkazi}"
                    Width="80"
                    Height="28"
                    Background="{DynamicResource PrimaryBrush}"
                    Foreground="{DynamicResource TextOnPrimaryBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    Click="Otkazi_Click"/>
        </StackPanel>

    </Grid>
</Window>
