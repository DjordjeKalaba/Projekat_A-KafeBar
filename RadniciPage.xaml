<Page x:Class="Projekat_A_KafeBar.RadniciPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Radnici">

    <Grid Background="{DynamicResource BackgroundBrush}" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Dugmad za akcije -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,10">
            <Button Content="{DynamicResource DodajRadnikaText}" Width="120" Margin="5"
                    Background="{DynamicResource PrimaryBrush}" 
                    Foreground="{DynamicResource ForegroundBrush}" 
                    BorderBrush="{DynamicResource BorderBrush}" 
                    Click="DodajRadnika_Click"/>
            <Button Content="{DynamicResource ObrisiRadnikaText}" Width="120" Margin="5"
                    Background="{DynamicResource PrimaryBrush}" 
                    Foreground="{DynamicResource ForegroundBrush}" 
                    BorderBrush="{DynamicResource BorderBrush}" 
                    Click="ObrisiRadnika_Click"/>
            <Button Content="{DynamicResource IzmijeniRadnikaText}" Width="120" Margin="5"
                    Background="{DynamicResource PrimaryBrush}" 
                    Foreground="{DynamicResource ForegroundBrush}" 
                    BorderBrush="{DynamicResource BorderBrush}" 
                    Click="IzmijeniRadnika_Click"/>
        </StackPanel>

        <!-- Prikaz liste radnika -->
        <DataGrid Name="RadniciDataGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True"
                  Background="{DynamicResource BackgroundBrush}"
                  Foreground="{DynamicResource ForegroundBrush}"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="{DynamicResource BorderBrush}"
                  VerticalGridLinesBrush="{DynamicResource BorderBrush}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{DynamicResource ImeText}" Binding="{Binding Ime}" Width="*"/>
                <DataGridTextColumn Header="{DynamicResource PrezimeText}" Binding="{Binding Prezime}" Width="*"/>
                <DataGridTextColumn Header="{DynamicResource KorisnickoImeText}" Binding="{Binding KorisnickoIme}" Width="*"/>
                <DataGridTextColumn Header="{DynamicResource UlogaText}" Binding="{Binding Uloga}" Width="*"/>
                <DataGridTextColumn Header="{DynamicResource BrojTelefonaText}" Binding="{Binding BrojTelefona}" Width="*"/>
                <DataGridTextColumn Header="{DynamicResource PlataText}" Binding="{Binding Plata}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
