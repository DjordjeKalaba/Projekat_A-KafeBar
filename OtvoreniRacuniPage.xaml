<Page x:Class="Projekat_A_KafeBar.OtvoreniRacuniPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Projekat_A_KafeBar"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="500"
      Title="{DynamicResource OtvoreniRacuni_Title}">

    <Grid Background="{DynamicResource BackgroundBrush}" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Naslov -->
        <TextBlock Text="{DynamicResource OtvoreniRacuni_Title}" 
                   FontSize="30" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,20"
                   Foreground="{DynamicResource ForegroundBrush}"/>

        <!-- DataGrid -->
        <DataGrid x:Name="RacuniDataGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                  Background="{DynamicResource BackgroundBrush}"
                  Foreground="{DynamicResource ForegroundBrush}"
                  BorderBrush="{DynamicResource BorderBrush}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{DynamicResource OtvoreniRacuni_Col_ID}" Binding="{Binding IdRačuna}" Width="50"/>
                <DataGridTextColumn Header="{DynamicResource OtvoreniRacuni_Col_Sto}" Binding="{Binding Sto}" Width="100"/>
                <DataGridTextColumn Header="{DynamicResource OtvoreniRacuni_Col_Iznos}" Binding="{Binding Iznos}" Width="100"/>
                <DataGridTextColumn Header="{DynamicResource OtvoreniRacuni_Col_Vrijeme}" Binding="{Binding VrijemeIzdavanja}" Width="*"/>
                <DataGridTextColumn Header="{DynamicResource OtvoreniRacuni_Col_Zaposleni}" Binding="{Binding Zaposleni}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Dugme -->
        <Button Grid.Row="2" Content="{DynamicResource OtvoreniRacuni_Button_Azuriraj}" 
                Width="150" Height="40" HorizontalAlignment="Center" Margin="0,20,0,0"
                Background="{DynamicResource PrimaryBrush}" 
                Foreground="{DynamicResource ForegroundBrush}"
                Click="AzurirajRacun_Click"/>
    </Grid>
</Page>
